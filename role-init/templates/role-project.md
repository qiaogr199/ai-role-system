# Role Project Config

> Generated by `role-init`. Project-specific binding for the generic 7-role system.

## Identity

| Key | Value |
|-----|-------|
| project_name | {PROJECT_NAME} |
| initialized | {YYYY-MM-DD} |
| stack | {e.g. Node 20, pnpm, Turbo monorepo} |

## Paths

| Key | Path |
|-----|------|
| agents_map | `AGENTS.md` |
| handoff_root | `doc/ai-handoffs` |
| handoff_templates | `doc/ai-handoffs/_templates` |
| handoff_index | `doc/ai-handoffs/_index.md` |
| local_spec | `{optional, e.g. docs/ai-roles.md}` |
| db_scripts | `{optional, e.g. doc/db/ or migrations/}` |

## Commands

| Key | Command |
|-----|---------|
| build | `{BUILD_CMD}` |
| test | `{TEST_CMD}` |
| test_scoped | `{SCOPED_TEST_CMD or same as test}` |

Document placeholders like `{module}` if used.

## Shared / high-risk modules

List paths or packages that require `/arch` + downstream impact list when changed:

- `{module-or-package-1}`
- `{module-or-package-2}`

Use `none` if monolith or no shared layers.

## Global red lines

- {RED_LINE_1}
- {RED_LINE_2}
- Schema/data changes require `/arch` + documented migration path
- Do not commit secrets

## Migration (optional)

| Key | Value |
|-----|-------|
| migration_doc | `{path or none}` |
| strategy | `{one line or none}` |

## Core entry points (optional)

List public API / route / handler classes for destructive-change checklist:

| Domain | Entries |
|--------|---------|
| {domain} | {ClassOrRoute names} |

Use `none` if not applicable.

## QA notes

- {how to run tests, coverage expectations, e2e if any}

## Init history

| Date | Action |
|------|--------|
| {YYYY-MM-DD} | Initial role-init |
